#include <stdio.h>

int k, l, m;

int main()
{
    printf("Wwedite dliny storon 4erez probel\n");
    scanf("%d %d %d", &k, &l, &m);
    
    if (k+l>m && l+m > k && m+k > l)
    {
     printf("Yes \n");
     }
     else if (printf("No \n"));   
    return 0;
}
